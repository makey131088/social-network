[tool.poetry]
name = "lesson-8"
version = "0.1.0"
description = ""
authors = ["Alexander <makal@inbox.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.1.3"
gunicorn = "^26.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
